name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: |
          echo "hello world"
      - name: This will fail
        run: exit 1;
